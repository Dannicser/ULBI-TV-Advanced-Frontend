import { profileActions, profileReducer } from "./model/slice/profileSlice";
import { IProfileSchema, IProfile } from "./model/types/profile";

export { IProfileSchema, IProfile, profileActions, profileReducer };
