{
  "presets": [["@babel/preset-env", { "targets": { "node": "current" } }], "@babel/preset-typescript"],
  "plugins": [
    [
      "@babel/plugin-transform-react-jsx",
      {
        "runtime": "automatic" // без импорта react в каждом файле
      }
    ]
  ]
}

// необходим для работы jest с typescript

//@babel/plugin-transform-react-jsx - необходим для тестирования реакта
